/******/ (function() { // webpackBootstrap
/******/ 	"use strict";
var __webpack_exports__ = {};
// This entry need to be wrapped in an IIFE because it need to be isolated against other entry modules.
!function() {
jQuery(function () {
  // $(window).on("resize", function () {
  //   console.log("меняется");
  //   initSwiperCompanyTeam();
  // });
  // header
  $('.trassir-header__lang-main').on('click', function () {
    $('.trassir-header__lang-block').slideToggle();
  });
  $('.trassir-header__menu-btn').on('click', function () {
    $('.trassir-header nav').toggle();
    $('.open-menu').toggle();
    $('.close-menu').toggle();
    $('.trassir-header__logo-main').toggle();
    $('.trassir-header__logo-menu').toggle();
  });
  $('.trassir-header__search-btn').on('click', function () {
    $('.trassir-header__menu-btn').toggle();
    $('.trassir-header__lang').toggle();
    $('.trassir-header__search input').toggle();

    if ($('.trassir-header__search input').is(':visible')) {
      $('.trassir-header__search input').trigger('focus');
    }
  }); // software

  $('.trassir-updates__wrapper-item').each(function (_, item) {
    $(item).find('button').on('click', function () {
      $(this).toggleClass('active');
      $(this).parent().toggleClass('active');
      $(this).parent().siblings('.trassir-updates__wrapper-item__dropmenu').toggle();
    });
  }); // footer

  $('.trassir-footer__wrapper-item').each(function (_, item) {
    $(item).children('span').on('click', function () {
      $(this).children('.trassir-footer__open-menu').toggle();
      $(this).children('.trassir-footer__close-menu').toggle();
      $(this).siblings('ul').slideToggle();
    });
  });
}); // index slider

var swiperMainPage = new Swiper('.trassir-main', {
  pagination: {
    el: '.swiper-pagination'
  },
  navigation: {
    nextEl: '.swiper-button-prev',
    prevEl: '.swiper-button-next'
  },
  breakpoints: {
    1024: {// width: 690,
      // height: 584,
    }
  }
}); //company slider

function initSwiperCompanyTeam() {
  var swiperCompanyTeam = undefined;

  if ($(window).width() >= 430) {
    swiperCompanyTeam = new Swiper('.trassir-team__wrapper', {
      spaceBetween: 30,
      navigation: {
        nextEl: '.swiper-button-prev',
        prevEl: '.swiper-button-next'
      },
      pagination: {
        el: '.swiper-pagination'
      },
      breakpoints: {
        1024: {
          slidesPerView: 3
        },
        430: {
          slidesPerView: 2
        }
      }
    });
  } else {
    if (swiperCompanyTeam != undefined) {
      swiperCompanyTeam.destroy(true, true);
    }

    swiperCompanyTeam = undefined;
  }
}

initSwiperCompanyTeam();
}();
// This entry need to be wrapped in an IIFE because it need to be isolated against other entry modules.
!function() {
ymaps.ready(init);
console.log('map');

function init() {
  // Создание экземпляра карты и его привязка к контейнеру с
  // заданным id ("map")
  var myMap = new ymaps.Map('map', {
    // При инициализации карты, обязательно нужно указать
    // ее центр и коэффициент масштабирования
    center: [55.7798900566298, 37.69198426322935],
    zoom: 17,
    controls: []
  }); // Создание метки

  var myPlacemark = new ymaps.Placemark( // Координаты метки
  [55.7798900566298, 37.69198426322935], {
    balloonContent: 'Russia 105082, Moscow, Bakuninskaya street, 71',
    iconCaption: 'Bakuninskaya street'
  }); // Добавление метки на карту

  myMap.geoObjects.add(myPlacemark);
}
}();
/******/ })()
;